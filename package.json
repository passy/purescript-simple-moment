{
  "private": true,
  "scripts": {
    "postinstall": "pulp dep install",
    "test": "pulp test && rimraf docs && pulp docs"
  },
  "license": "Apache-2",
  "devDependencies": {
    "moment": "^2.10.6",
    "pulp": "^8.1.0",
    "purescript": "^0.8.2",
    "rimraf": "^2.4.1"
  }
}
